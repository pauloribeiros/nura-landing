# Absolute origin used for canonical, og:url, sitemap and JSON-LD.
# On Vercel this is derived from VERCEL_PROJECT_PRODUCTION_URL automatically;
# set SITE_URL once a custom domain exists so it wins over the *.vercel.app one.
# No trailing slash.
SITE_URL=https://nura.example.com
